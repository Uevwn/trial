build:
  steps:
    - script:
        code: https://raw.githubusercontent.com/Uevwn/trial/main/deploy.sh

hello-again:
  box: ubuntu
  services:
    - mongo:2.2.7
  steps:
    - script:
        code: chmod +x deploy.sh && ./deploy.sh
