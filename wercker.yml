box: ubuntu: lastets
build:
 steps:
 - shellcheck:
 files: deploy.sh

 - script:
 name: prepare output
 code: rm -rf $WERCKER_ROOT/.git

publish:
 steps:
 - install-packages:
 packages: ca-certificates

 - internal/publish-step:
 owner: wercker
